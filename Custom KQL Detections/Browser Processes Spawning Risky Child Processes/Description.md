Detects web browsers spawning high-risk child processes commonly used in
SVG/HTML smuggling attacks. These include script interpreters (wscript,
cscript, mshta) and Living-off-the-Land binaries (LOLBins) like PowerShell,
cmd.exe, rundll32, and curl.
